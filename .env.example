# Copy to .env.local and fill in your values (see SETUP.md)

# Required for auth + app (Supabase Dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required for the AI chat (console.groq.com → API keys)
# GROQ_API_KEY=

# Vercel Blob (optional, for file uploads)
# BLOB_READ_WRITE_TOKEN=
